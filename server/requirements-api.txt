# FastAPI 서버(main.py)가 실제로 쓰는 패키지만.
# librosa / torch / demucs 는 Celery 워커 전용이라 여기 없음.
# 버전은 EC2 운영 환경(~ensemble/venv)과 동일하게 고정.
fastapi==0.136.1
uvicorn==0.46.0
websockets==16.0
python-multipart==0.0.29
pydantic==2.13.4
psycopg2-binary==2.9.12
celery==5.6.3
redis==7.4.0
python-dotenv==1.0.1
